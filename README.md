# Topperoo
## Requirements
* Virtualbox
* Vagrant

## Run
* `cd $working_directory`
* `vagrant up`
* `vagrant ssh`
* `cd /vagrant`
* Run the static webserver: `node server.js`
* Compile assets: `gulp`
* Open in browser: `http://localhost:3000`
